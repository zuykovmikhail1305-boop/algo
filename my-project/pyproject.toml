[project]
name = "my-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fr1ze", email = "Mizu1305@yandex.ru" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
]

[project.scripts]
my-project = "my_project:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
